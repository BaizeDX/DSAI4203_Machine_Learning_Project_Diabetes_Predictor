### Transition Summary

Experiment 11 established the core methodology: 5-fold stratified cross-validation with fold-wise categorical encoding, OOF evaluation, and early stopping. The notebook version successfully validated the approach, achieving consistent CV AUC of 0.7256 with low variance.

To transform this validated notebook into a production-ready pipeline, we extracted the logic into a modular Python package (`src/`). The main entry point `src/train_xgb_cv.py` now encapsulates:

- Data loading and feature engineering
- Baseline model evaluation (Dummy, Decision Tree)
- XGBoost training with proper CV
- Result saving and submission generation

The module produces identical results to the notebook, confirming successful migration.

---

### Verification

| Component | Notebook (11) | Module (src/train_xgb_cv.py) | Status |
|-----------|---------------|------------------------------|--------|
| Fold 1 AUC | 0.72596 | 0.72596 | ✓ Identical |
| Fold 2 AUC | 0.72428 | 0.72428 | ✓ Identical |
| Fold 3 AUC | 0.72515 | 0.72515 | ✓ Identical |
| Fold 4 AUC | 0.72675 | 0.72675 | ✓ Identical |
| Fold 5 AUC | 0.72591 | 0.72591 | ✓ Identical |
| OOF AUC | 0.72560 | 0.72560 | ✓ Identical |
| Feature Importance | Same ranking | Same ranking | ✓ Identical |

---

### How to Run

```bash
# Module version (produces identical results)
python -m src.train_xgb_cv

# Notebook version
jupyter notebook notebooks/11_xgboost_cv_final.ipynb
```

Both produce the same outputs in `submissions/`, `logs/`, and `figures/`. The module version is now the recommended entry point for reproducibility and further development.
